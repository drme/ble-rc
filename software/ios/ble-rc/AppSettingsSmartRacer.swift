import Foundation;
import UIKit;

class AppSettings: NSObject
{
	static let applicationTint = UIColor(red: 112.0 / 255.0, green: 179.0 / 255.0, blue: 71.0 / 255.0, alpha: 1);
	static let addMessage = "The cars are listed as they are discovered in the table below. If there are no cars, you can";
	static let shopForCarsUrl = "https://github.com/drme/ble-rc/";
	static let applyHue = false;
}
