import Foundation

/*
class FakeFirmwareUpdater: FirmwareUpdater
{
	init()
	{
		super.init(device: nil);
	}

	override func startUpdate(firmwareFile: FirmwareFile, failedHandler: Event, succeededHandler: Event, disconnectedHandler: Event, badImageHandler: EventBadImage, progressHandler: EventProgress)
	{
		super.startUpdate(firmwareFile, failedHandler: failedHandler, succeededHandler: succeededHandler, disconnectedHandler: disconnectedHandler, badImageHandler: badImageHandler, progressHandler: progressHandler);

	//	self.requestedBlock = -1;

//		NSTimer.scheduledTimerWithTimeInterval(0.001, target: self, selector: "simulateUpdate", userInfo: nil, repeats: false);
	}

	func simulateUpdate()
	{
	//	self.requestedBlock++;
/*
		if (self.iBlocks < self.nBlocks)
		{
			let timer = NSTimer.scheduledTimerWithTimeInterval(0.001, target: self, selector: "simulateUpdate", userInfo: nil, repeats: false);
			programmingTimerTick(timer);
		} */
	}
}

*/