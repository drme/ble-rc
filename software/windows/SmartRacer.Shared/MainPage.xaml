<phone:PhoneApplicationPage
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:shared="clr-namespace:SmartRacer.Shared;assembly=SmartRacer.Shared"
    x:Class="SmartRacer.MainPage"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True" Loaded="PageLoaded">

    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True" BackgroundColor="#FF8CC63F" ForegroundColor="White" Mode="Minimized">
            <shell:ApplicationBarIconButton x:Name="connectButton" IconUri="/Assets/transport.play.png" Text="connect" Click="ConnectButtonClicked"/>
            <shell:ApplicationBarIconButton x:Name="disconnectButton" IconUri="/Assets/cancel.png" Text="disconnect" Click="DisconnectButtonClicked"/>
            <shell:ApplicationBarIconButton x:Name="settingsButton" IconUri="/Assets/edit.png" Text="settings" Click="SettingsButtonClicked"/>
            <shell:ApplicationBarIconButton x:Name="statusButton" IconUri="/Assets/folder.png" Text="status" Click="StatusButtonClicked"/>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="257*"/>
            <ColumnDefinition Width="223*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="0,0,0,696" Grid.ColumnSpan="2" Grid.RowSpan="2"/>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="0,0,0,0" Grid.ColumnSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="71*"/>
            </Grid.ColumnDefinitions>

            <shared:ThumbStick x:Name="steeringSlider" HorizontalAlignment="Left" Margin="60,0,0,0" VerticalAlignment="Top" Moved="SteeringChanged" Opacity="0.3" Width="305" Height="352" Grid.Column="1"/>
            <shared:ThumbStick x:Name="throttleSlider" HorizontalAlignment="Left" Margin="10,357,0,0" VerticalAlignment="Top" Orientation="Vertical" Moved="ThrottleChanged" Opacity="0.3" Width="470" Grid.ColumnSpan="2"/>
            <Image x:Name="connectionStatus" Grid.Column="1" HorizontalAlignment="Left" Height="32" Margin="403.851,10,0,0" VerticalAlignment="Top" Width="32" Source="/Assets/bt-ok.png" RenderTransformOrigin="0.5,0.5" UseLayoutRounding="False" d:LayoutRounding="Auto" Opacity="0.75">
                <Image.RenderTransform>
                    <CompositeTransform Rotation="90"/>
                </Image.RenderTransform>
            </Image>


        </Grid>

    </Grid>

</phone:PhoneApplicationPage>